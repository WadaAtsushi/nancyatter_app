<div class="container w-75 mt-5">
  <%= tag.h1 "投稿一覧" %>
    <table class="table">
      <thead>
        <%= tag.tr %>
          <%= tag.th "title" %>
          <%= tag.th "content" %>
      </thead>
      　
      <tbody>
        <% @posts.each do |post| %>
          <%= tag.tr %>
            <%= tag.td link_to("#{post.title}", post_show_path(post.id) ) %>
            <%= tag.td post.content %>
        <% end %>    
      </tbody>
    </table>
</div>
